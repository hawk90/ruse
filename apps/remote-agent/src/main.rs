//! ruse-remote-agent — headless remote workspace agent (--stdio) (thin frontend; engine lives in crates/).
fn main() {
    eprintln!("ruse-remote-agent: spec-first skeleton — not yet implemented");
}
