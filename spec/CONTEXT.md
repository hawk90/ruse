<!--
  STATUS: MANUALLY MAINTAINED (interim).
  This file is INTENDED to be generated by `xtask context build` from PROJECT.md, PRD.yaml,
  ARCHITECTURE.md, POLICY.yaml, DECISIONS.md, and context-profiles.yaml (see DECISIONS D-021/D-022).
  Until that generator exists, it is hand-written — do NOT trust it over the sources; when they disagree,
  the sources win. Once the generator lands, this becomes read-only/generated.
-->

# Project Context (compact pack for LLMs)

Read this first. Follow an ID into the sources only when the task needs that detail. For task-scoped packs
see [context-profiles.yaml](context-profiles.yaml).

## Goal
Terminal-first, remote-first, extensible editor with Vim/Neovim/Emacs feature parity, built as a
specification + reference implementation.

## Current stage
Pre-code. Design/spec phase. No repo/crates yet.

## Core invariants (must hold)
- Document does not depend on View. (INV-DOC-VIEW)
- All document mutations go through Transactions with revision + origin. (INV-TXN)
- Positions are typed; long-lived positions are anchors. (INV-POS-TYPED, INV-ANCHOR)
- Plugins are isolated over a versioned protocol; never touch core types. (INV-PLUGIN-*)
- Core is a single-threaded deterministic executor; stale async results dropped. (INV-ASYNC-ORDER)
- Expected failures = typed errors; impossible states = asserts; log once at boundaries. (INV-ERR-CLASS)
- Capabilities/features degrade, they do not disappear. (INV-CAP-DEGRADE)
- One fact, one home: state in spec/ YAML, prose in docs/, generated files not hand-edited. (ENG-DOC-001)

## Primary flow
Input → Input Profile → Semantic Command → Transaction → Document(rev++) → View Model → Render Tree →
Frontend Lowering. (ARCH-FLOW-001)

## Active requirements (MVP)
- F-001 Transactional editing · F-002 Document & coordinate model · F-003 Vim input profile
- F-004 Semantic command engine + palette · F-005 Undo grouping · F-006 TUI rendering
- F-007 Buffers/views/windows · F-008 Save + crash recovery · F-009 Search · F-010 Terminal capability
(full list + post-MVP/future in [PRD.yaml](PRD.yaml))

## Active decisions
- Rust workspace, TUI-first. (D-020)
- Transaction-only mutation; deterministic single-thread core. (D-001, D-002)
- Plugins: WASM/process protocol, not Rust dylib; **no WASM host in MVP** (internal API first). (D-004, D-009)
- Client/runtime boundary first-class from day one. (D-011)
- spec/ (state) + docs/ (reference); generator is future work. (D-021, D-022)

## Open questions
- Save/recovery journal format + undo-grouping boundaries. (D-005)
- Multi-client workspace + offline/reconnect policy. (D-012, D-013)
- Log PII/redaction field list. (D-017)

## Where things live
`spec/`: PROJECT (vision/glossary), PRD.yaml (features), ARCHITECTURE.md (map), POLICY.yaml (enforced
rules), DECISIONS.md (hard choices). `docs/`: deep design, parity research, full anti-pattern catalog,
invariants, protocols, operations. Templates in `spec/templates/`.
