"""Tests for the change-workflow toolchain. Run: python3 -m unittest discover -s tools/tests

Stdlib unittest only — no test-framework dependency (dependency policy D-034: a dev dep
would need a spec/dependencies.yaml entry; stdlib needs none).
"""
