# Release BUILD (build once; publish separately). Activates when crates/ builds real binaries.
# See docs/operations/ci-cd-and-release.md §6 (assets: per-platform binaries + SHA256SUMS + SBOM + provenance).
name: release
on:
  push: { tags: ['v*'] }
permissions: { contents: write }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4          # TODO pin SHA
      - run: |
          echo "TODO: cross-platform build + test + SHA256SUMS + SBOM + provenance -> draft release."
          echo "Rollback = re-publish a prior verified artifact (never a fresh build)."
