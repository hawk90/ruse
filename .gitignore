# Rust
/target/
**/*.rs.bk
*.pdb
# NOTE: Cargo.lock IS committed — this workspace ships binaries (apps/*).

# Python tooling
__pycache__/
*.pyc

# Editors / OS
.DS_Store
.idea/
*.swp

# Local / secrets / artifacts
.env
.env.*
/dist/
diagnostic-bundle/
*.log

# ruse: local change-workspace state (change.yaml/plan/context/impact/evidence).
# Only the final RFC/Decision/PRD/PR is permanent — see docs/operations/change-workflow.md.
/.ruse/

# ruse: generated spec artifacts (regenerated by xtask, D-022) — uncomment when the generator exists
# spec/CONTEXT.md
# spec/glossary.json
# .context/
